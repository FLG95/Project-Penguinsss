

#include "deps/display.h"
#include "time.h"


int main(){

    Tile** board = NULL;
    Player* players;
    WINDOW* window;

    srand(time(NULL));

    makeWindow();

    //players = createPlayers();

    //board = createBoard();

    //checkfish = checkFish(board, players);

    //showBoard(board, window);
    //showScore(players);




    //free(players);
    //free(board);
    return 0;

}